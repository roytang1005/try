import prompts from 'prompts';

async function main(): Promise<void> {
  let targetVersion: string | undefined;

  // const {} = await prompts({});
}

main().catch((err) => {
  console.error(err);
  process.exit();
});
